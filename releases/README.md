# Cork Robotics Releases

Versioned firmware and desktop artifacts live here.

- Firmware: `/releases/firmware/<board-id>/<version>/<board-id>.bin`
- Desktop: `/releases/desktop/<platform-id>/<version>/<installer-or-archive>`

Keep artifact paths immutable after publishing. To replace a build, publish a new version and update the matching manifest in `/updates`.
